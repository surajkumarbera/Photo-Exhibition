const path = require("path");

// constants
const HOME_PAGE_PATH = path.join(__dirname, "../", "../", "Client", "homePage.html");
const IMAGEE_FILE_PATH = path.join(__dirname, "../", "../", "Data");

//export modules
module.exports = {
    HOME_PAGE_PATH,
    IMAGEE_FILE_PATH
};